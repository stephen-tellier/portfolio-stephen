import { ParallaxV2Directive } from './parallax-v2.directive';

describe('ParallaxV2Directive', () => {
  it('should create an instance', () => {
    const directive = new ParallaxV2Directive();
    expect(directive).toBeTruthy();
  });
});
